import React from "react";
import { useTitle } from "../../hooks/useTitle";
import "./index.scss";
const AccountOrderPage = () => {
  useTitle("Accout order");

  return <main>đây là order</main>;
};

export default AccountOrderPage;
